# Port du serveur (défaut: 3000)
PORT=3000

# Secret pour la signature des JWT (générer une chaîne aléatoire longue en production)
JWT_SECRET=changez-moi-en-production-chaine-aleatoire-longue

# Étape 4 : clé API Finnhub
FINNHUB_API_KEY=

# Étape 5 : base de données (PostgreSQL). Si absent, les comptes restent en mémoire.
# DATABASE_URL=postgresql://user:password@localhost:5432/simplifiedaction
